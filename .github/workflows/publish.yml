name: GitHub Actions

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@master
      - name: Setup dotnet core 2.2.105
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 2.2.105
      - name: Build with dotnet
        run: dotnet build
      - name: Package the build
        run: dotnet pack
      - name: Get the version
        id: get_version
        run: echo ::set-output name=VERSION::${GITHUB_REF}
      - name: Publish to Nuget
        run: dotnet nuget push hip-library/bin/Debug/hip-library.${{ steps.get_version.outputs.VERSION }}.nupkg -k ${{secrets.NUGET_API_KEY}} -s https://api.nuget.org/v3/index.json
